# Recovery Harness Request

- Harness source: `tmp/audit-evidence/week01-harness-candidate/recovery-harness.sh` (ignored, immutable, non-executable).
- Harness SHA-256: `e52606f25197691d184150fde0e8e62bac8c6e1c6ff8c505689673beebcabfd8` (258 lines).
- Contract: independent audit must verify runtime `PARENT_TOKEN` matching `sha256:<64hex>` and receipt binding (no embedded parent token), initialized pre-trap state, atomic unique roots, generated A/B workspace configs with exact Python `tomllib` paths and byte-equal sequential ports, JSON argv/credential sanitization, exact `cd &&` wrapper, complete per-operation/category JSONL IDs, full invariant categories, A stop/port absence, fresh-B empty proof before restore, enumerated custom backup plus roles/ownership/grants, pinned tool/image/migration/backup evidence, isolated no-network tooling, fail-closed secret scan, hard deadline cleanup, dirty-worktree/image/path guards, atomic no-manifest failure behavior, and non-executing self-tests.
- Verifier request: do not execute during this build; independently inspect source and SHA, then authorize only a temporary self-removing executable copy if every static contract and the exact lifecycle/receipt/category/equality behavior is proven.
- Recovery remains pending; no provider evidence, image, migrations, credentials, or tracked product files are to be changed.
