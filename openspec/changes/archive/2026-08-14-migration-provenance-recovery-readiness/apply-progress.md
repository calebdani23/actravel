# Apply Progress: Migration Provenance and Recovery Readiness

**Mode:** Standard; **delivery:** single PR with maintainer-approved `size:exception`; **work unit:** one bounded packet-only slice. The exception is limited to this read-only packet PR.

## Completed

- [x] Phase 1 (1.1–1.3): current identity, 59-file inventory, protected hashes, evidence contracts, and actual change-local spec path normalization.
- [x] Phase 2 (2.1–2.3): exact documentation-only archived-to-current Git classification, documentation/executable pairings, conservative exclusive discrepancy register, and distinct owner/required-authorizer boundaries.
- [x] Phase 3 (3.1–3.3): explicit unavailable recovery prerequisites, deterministic `BLOCKED` gate, and prohibited-operation boundary.
- [x] Phase 4 (4.1–4.3): bounded corrective RED/validation evidence, including exact archived source times and content hashes; no database or remote execution.

## Evidence

Focused check and RED results are recorded in `validation.md`. The exact local Git comparison proves 14 tracked Markdown-only changes and zero migration/generated-type changes; historical/local divergence remains separate from changed-path classification. Exact archived source times and source-artifact hashes are retained without promoting them to current remote authority. The final gate is exactly `BLOCKED`; `0061+` remains unsafe because authoritative remote provenance, approved target, recovery rehearsal, cleanup, and sign-off are unavailable. Maintainer approval evidence: Engram observation `#1618`, `Approved provenance packet size exception`, recorded 2026-08-11 in the parent OpenCode session; it explicitly selected “Approve exception” for one packet-only PR forecast at 300–420 lines within the 800-line session cap. No Git identity or signature is asserted; this is approval evidence only, not implementation proof.

## Rollback

Delete or supersede only the packet Markdown and this progress file. Protected migrations, `lib/supabase/database.types.ts`, the living spec, archived artifacts, and application/schema files remain unchanged.
