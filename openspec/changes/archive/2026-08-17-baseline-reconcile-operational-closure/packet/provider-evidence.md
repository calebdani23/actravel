# Provider Evidence

All records are append-only, read-only observations pinned to ref `bdyhakpmxegoipbmbtjb` and URL `https://bdyhakpmxegoipbmbtjb.supabase.co`. The URL guard ran before collection and matched exactly. Capture time: `2026-08-17T21:05:21Z`; collector: `sdd-apply correction executor`; authorizer: task-scoped read-only authorization. No secrets are recorded.

| ID | Evidence plane | Exact ref / URL | MCP operation or sanitized SQL locator | Status / limitations | Normalized result / reference | SHA-256 |
|---|---|---|---|---|---|---|
| `prov-identity-20260817-210521` | provider identity | `bdyhakpmxegoipbmbtjb` / `https://bdyhakpmxegoipbmbtjb.supabase.co` | `supabase_get_project_url` | PASS; does not prove production/staging/disposable role | URL exactly matched expected target; canonical record `ref\\nurl\\n` | `fa951b15c9e88b657c7cf8812f946c537b542081d611736a7705d4928e342c73` |
| `prov-ledger-20260817-210521` | authoritative remote migration ledger | same | MCP `supabase_execute_sql`; `select count(*) as row_count, md5(coalesce(string_agg(version || ':' || name, E'\\n' order by version, name), '')) as normalized_md5 from supabase_migrations.schema_migrations;` | PASS read-only; result is provider ledger, not local provenance; initial invalid `inserted_at` query exited tool error before reads | `row_count=59`, normalized MD5 `7024406f929d9ec597da5d0c547cf0f1`; includes `0051`, named `drop_public_rate_limits_write_policy`, `0044`–`0049`, `0053`–`0056`, `0058`–`0060`, excludes `0057` | `a61a3cd6587140ccdb763c6bbca07add255d59f24e5c39ec7a7367b9937c3704` |
| `prov-catalog-20260817-210521` | live catalog / behavior corroboration | same | MCP `supabase_execute_sql`; `select count(*) as row_count, md5(coalesce(string_agg(table_schema || '.' || table_name, E'\\n' order by table_schema, table_name), '')) as normalized_md5 from information_schema.tables where table_schema = 'public';` | PASS read-only; catalog cannot establish migration provenance | `row_count=35`, normalized MD5 `fdc1c75ab2dc1f8acab210ca44e1a31a`; not used as provenance | `b45cbea0f4621db866ccc8c7ee57372f9e9835e4ca160730b2076a36ae60b12e` |
| `prov-policy-20260817-210521` | live policy / behavior corroboration | same | MCP `supabase_execute_sql`; `select count(*) as row_count, md5(coalesce(string_agg(schemaname || '.' || tablename || ':' || policyname || ':' || permissive || ':' || array_to_string(roles, ',') || ':' || cmd, E'\\n' order by schemaname, tablename, policyname), '')) as normalized_md5 from pg_policies where schemaname = 'public';` | PASS read-only; policy state cannot establish migration provenance | `row_count=69`, normalized MD5 `8cf988b896173c464a98bfbe03812f8a`; not used as provenance | `d4a3ceac91d413dc8b4b11c5fc60a58f1ba51f042122bff8ed9950f268a974b4` |
| `prov-types-20260817-210521` | generated remote TypeScript payload | same | MCP `supabase_generate_typescript_types` | PASS read-only; generated output is comparison input only | Actual `types` string extracted from tool result, line-ending normalized only, saved to ignored `tmp/audit-evidence/baseline-reconcile-remote-types.ts` | `b6e3ea6876dd32c1e817d9f9f8ff7b28571a75ed5b29fd2faa5e10449b492637` |

The ledger tool-result SHA covers the MCP-captured response, including provider-returned migration statements. Catalog/policy response hashes are intentionally not invented from a truncated display; their exact SQL locators and session operation references are retained, while the ledger and generated payload have content-addressed files. Review status for every record: `unreviewed`; reviewer role `unassigned`; reviewed_at `unavailable`; evidence refs `identity.md`, `protected-snapshot.md`, `type-diff.md`.
