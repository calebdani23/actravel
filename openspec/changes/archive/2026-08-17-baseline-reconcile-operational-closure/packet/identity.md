# Identity Evidence

Captured at `2026-08-17T20:54:14Z` (UTC).

| Field | Evidence |
|---|---|
| Repository root | `/home/calebdani/srv/projects/actravel` (`git rev-parse --show-toplevel`) |
| HEAD | `7b0aeca160f1f6ed857374bc8982f57dca100241` |
| Git state | Pre-existing dirty worktree: `docs/about/helps/intakes/image.png` is modified; this change did not touch it. Packet directory is untracked. |
| Git selector guard | Absolute repository root accepted. No relative or foreign `-C` selector was used. Documentation-like paths were treated as data, never executable input. |
| Runtime | Node `v22.22.3`; npm `10.9.8`; package manager npm; lockfile v3. |
| Linked provider | Supabase ref `bdyhakpmxegoipbmbtjb`; URL `https://bdyhakpmxegoipbmbtjb.supabase.co`. Provider URL matched the required exact value before read-only collection. |
| Environment role | Not supplied or independently verified. This packet does not label the linked ref production, staging, or disposable. |
| Authorization | Read-only inspection authorized by the task; migration, schema, type overwrite, and provider mutation authorization absent. |

**Review:** owner `unassigned`; authorizer `task-scoped read-only authorization`; review status `unreviewed`; reviewer role/time `unassigned` / `not captured`.
